{
  "name": "taskflow-frontend",
  "version": "1.0.0",
  "description": "Taskflow frontend static server for Railway deployment",
  "scripts": {
    "build": "echo 'No build step required'",
    "start": "npx -y serve -l $PORT .",
    "preview": "npx -y serve -l $PORT ."
  }
}
